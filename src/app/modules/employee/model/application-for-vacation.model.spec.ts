import { ApplicationForVacation } from './application-for-vacation.model';

describe('ApplicationForVacation', () => {
  it('should create an instance', () => {
    expect(new ApplicationForVacation()).toBeTruthy();
  });
});
